<!-- example profile: demonstrates an `add` fragment. Harmless and generic. -->

## Custom-profile note (example)

This paragraph was appended to the base wiki writer prompt by the `example`
custom profile. It does not change any rule above — it only demonstrates that a
profile can layer extra guidance onto the default prompt without editing it.

If you can read this line inside the assembled writer prompt, the `add`
mechanism is working. Replace this file's content with real guidance in your own
profile.
